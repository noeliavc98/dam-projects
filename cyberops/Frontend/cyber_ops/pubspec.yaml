name: cyber_ops
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.7
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  shared_preferences: ^2.5.5
  flame: ^1.18.0
  http: ^1.2.0
  crypto: ^3.0.3


  # ── Música y Audio ────────────────────────────────
  just_audio: ^0.9.36
  flutter_tts: ^4.2.5
  
  
  otp: ^3.1.4
  qr_flutter: ^4.1.0
  base32: 2.1.3

  image_picker: ^1.0.7
  file_picker: ^8.1.2
  flutter_image_compress: ^2.1.0
  image: ^4.1.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/nivel_08/
    - assets/images/nivel_20/
    - assets/games/
    - assets/audio/
